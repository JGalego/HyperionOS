[package]
name = "hyperion-identity"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Who is acting: a principal bound to its own Trust Boundary (docs/998-roadmap.md §0 Decision 2). Identity, deliberately not authentication."

[dependencies]
hyperion-capability = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Proves this crate's own load-bearing claim -- that binding a user to a Trust Boundary makes the
# audit trail per-person -- against the real crate, which needed no change to make it true.
hyperion-explainability = { workspace = true }
tempfile = { workspace = true }
