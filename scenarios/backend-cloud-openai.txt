# A real, paid, external OpenAI API call -- needs a genuinely valid OPENAI_API_KEY in .env and
# this binary built with `--features openai-compat`. Connecting and switching in the same
# session grants this one running session immediate use -- no separate "yes/no" consent prompt
# fires here; that only happens on a *fresh* process reusing an already-stored key (see
# docs/999-usage-scenarios.md's cloud-provider notes for that path).
#
#   cargo build -p hyperion-console --bin hyperion-console --features openai-compat
#   set -a && source .env && set +a
#   rm -rf /tmp/hyperion-scratch
#   HYPERION_CONSOLE_DATA_DIR=/tmp/hyperion-scratch \
#       ./target/debug/hyperion-console scenarios/backend-cloud-openai.txt
#
# Edit "gpt-4o-mini" below if your account should use a different model. `/backend openai <model>`
# does a real, eager connectivity check right then -- an invalid key fails outright with a real
# 401 and the session stays on whichever backend was active before, rather than half-switching.

connect my openai account
$OPENAI_API_KEY

/backend openai gpt-4o-mini

what is the capital of France
