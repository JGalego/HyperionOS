# A real, paid, external Groq API call -- needs a genuinely valid GROQ_API_KEY in .env and this
# binary built with `--features openai-compat` (Groq's own API is OpenAI-wire-compatible, so it
# reuses that feature -- see hyperion_console::session::ConsoleSession::try_connect_groq).
# Connecting and switching in the same session grants this one running session immediate use --
# no separate "yes/no" consent prompt fires here; that only happens on a *fresh* process reusing
# an already-stored key.
#
#   cargo build -p hyperion-console --bin hyperion-console --features openai-compat
#   set -a && source .env && set +a
#   rm -rf /tmp/hyperion-scratch
#   HYPERION_CONSOLE_DATA_DIR=/tmp/hyperion-scratch \
#       ./target/debug/hyperion-console scenarios/backend-cloud-groq.txt
#
# Edit "llama-3.3-70b-versatile" below if your account should use a different model. `/backend
# groq <model>` does a real, eager connectivity check right then -- an invalid key fails outright
# and the session stays on whichever backend was active before, rather than half-switching.

connect my groq account
$GROQ_API_KEY

/backend groq llama-3.3-70b-versatile

what is the capital of France
